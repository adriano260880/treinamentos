type Query {
    user(id: ID!): User
    users: [User]
}

type User {
    id: ID
    name: String
    orders: [Order]
}

type Order {
    id: ID
    total: Float
    products: [Product]
}

type Product {
    id: ID
    name: String
    price: Float
}